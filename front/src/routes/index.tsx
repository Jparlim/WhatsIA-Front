import { BrowserRouter } from "react-router-dom";
import { Rotas } from "./Routes";

export function Routes() {
    return (
        <BrowserRouter>
            <Rotas/>
        </BrowserRouter>
    )
}